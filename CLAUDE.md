# kakera

Claude Code セッションを跨いだ長期記憶 / 自己学習システム。
このファイルは plugin として読み込まれ、Claude の振る舞いを kakera プロトコルに適応させる。

Vault パス: 環境変数 `$KAKERA_HOME` (default `~/kakera`)。以下では `$KAKERA_HOME` と表記。

## 目的

ユーザーが業務の中で学んだことを、自然な対話の中で蓄積し、次の対話で自然に再会できるようにする。
ユーザーに「メモを書く」「タグを選ぶ」等の摩擦を強いない。入力は自然言語のみ。

## 入力プロトコル (自然言語トリガー)

ユーザーが対話の中で以下のような発話をしたら、その時点までの会話文脈から該当箇所を抽出して保存する。
ユーザーに「どこに保存するか」「カテゴリは何か」を尋ねず、Claude が判断する。判断後に「これを `<カテゴリ>` に保存しました: `<ファイル名>`」と短く報告する。

| トリガー語 (例) | 想定カテゴリ | 振る舞い |
|---|---|---|
| 「これメモ」「覚えといて」「残しておいて」 | 文脈から判定 | 直近の会話から要点を 1 つ抽出し、適切なカテゴリへ保存 |
| 「同じミスしたくない」「またやらかした」「これ罠だった」 | `mistakes/` | 原因と再発防止策の構造で保存 |
| 「ここ分からないままにしたくない」「あとで調べたい」「今は 8 割で進める」 | `questions/` | 未解決の問いとして保存 (decay: 1month) |
| 「これは原則として残す」「次回からこう判断する」 | `feedback/` または `design/` | 行動指針 / 設計原則として保存 |
| 「この技術選定の理由を残しておきたい」「この決定の根拠」 | `decisions/` | 結論・理由・棄却案の構造で保存 |

トリガー語の表記揺れは寛容に判定する。明らかに学びの保存要求と読める発話を取りこぼさない。

## 受動再会プロトコル

session 中、関連する既存知見があれば Claude が自然に想起する。

- **再会スタイル**: `$KAKERA_HOME/.kakera-config.toml` の `recall_style` で決まる。`explicit` (明示多め) と `implicit` (明示少なめ) の 2 種類。`/kakera-init` で設定する。default は `implicit`。
- **explicit**: 関連知見が見つかったら「以前 `[[X]]` でこう判断しています、今回はどう?」と明示的に言及する。
- **implicit**: importance: high または矛盾発生時のみ明示。それ以外は黙って context に取り込む。
- **session 開始時**: ユーザーが最初の話題を出したら、関連カテゴリの hub note (`$KAKERA_HOME/knowledge/<category>/<hub>.md`) を Read して関連度の高いノートを把握しておく。

## 検索プロトコル (Claude が記憶を引く時)

ユーザーが過去の判断・知見を参照する話題を出したとき、以下のトリガーで kakera を検索する。

- 「前に」「以前」「あの時」「前回」「あのとき決めた」など過去参照
- 設計判断 / 技術選定 / 方針決定の議論
- バグ調査で過去類似事例の可能性
- ユーザーの提案が既に却下された方針かもしれない

ユーザーに質問する前に、まず `$KAKERA_HOME/knowledge/` を検索する。既存の解決策や過去の議論を踏まえてから提案を組み立てる。

検索のスキップ条件
- typo 修正 / スタイル変更など設計判断を伴わない作業
- 初めて扱うことが明らかなトピック

## 書き込みプロトコル

新規ファイルを作る・既存を更新する手順。

### 1. 既存メモリの確認

新規作成の前に必ず以下を確認する。

- `$KAKERA_HOME/MEMORY.md` (インデックス)
- 該当カテゴリの hub note
- `ls $KAKERA_HOME/knowledge/*/` で既存ファイル一覧
- 関連しそうな既存 member ノートを Read

### 2. Surprise 3 段判定

新しい知見は既存メモリとの関係で次のいずれかに分類してから書く。

- **整合 (low surprise)**: 既存と 90% 超重複・新情報なし → 新規作成しない。既存ノートの `references` に今日の日付を追記して「再確認」記録
- **補強 (medium surprise)**: より一般化できる / 反例 / 適用条件が広がる → 既存ファイルを Edit し description・本文を更新、`references` に日付追記
- **矛盾・新概念 (high surprise)**: 既存の前提を覆す / 新しいカテゴリの知見 → 新規ファイル作成

### 3. ファイル命名規則 (厳守)

- ファイル名 = 日本語タイトルそのもの (例: `プロダクトコードにはWHYを残す.md`)
- カテゴリ prefix (`feedback_` `design_` 等) は付けない。カテゴリはフォルダで表現
- `decisions/` `mistakes/` のみ `YYYY-MM-DD_` の日付 prefix を付ける
- wikilink `[[name]]` は実ファイル名 (拡張子・パスなし) と完全一致。kebab/snake/英訳など揺れ厳禁

### 4. frontmatter (必須項目)

```yaml
---
name: ファイル名と同じ
description: 一行要約 (検索で関連度判断に使われる)
type: feedback | design | user | project | decision | mistake | question
importance: high | medium | low
created: YYYY-MM-DD
decay: 1month | 3months | 6months | permanent
references:
  - YYYY-MM-DD
---
```

importance の指針
- `high`: 毎回適用される判断軸
- `medium`: 繰り返し使う知見
- `low`: 単発寄りの経験則

decay の指針
- `permanent`: 設計哲学・行動原則など減衰しない
- `6months`: アーキテクチャ判断、技術選定
- `3months`: プロジェクト固有の設計判断
- `1month`: 一時的な制約、暫定対応、未解決の問い

### 5. サブ hub への振り分け

- 親 hub に `### サブ hub` セクションがある場合、新ノートが既存サブ hub のテーマに該当するかを判断
- 該当する → そのサブ hub の `## メンバー` セクションに `[[新ノート]]` を追記。新ノート末尾は `戻る: [[サブhub名]]`
- 該当せず、同カテゴリに同テーマで 3 件以上の関連ノートが溜まる → 新規サブ hub (`type: sub-hub`) を作成して集約
- 該当も類似集積もなければ → 親 hub 直結。`戻る: [[親hub]]`
- 親 hub のメンバーリストは触らない (`bin/regen-hubs.py` が自動的にサブ hub と直結を区別表示する)

### 6. 矛盾リンク慣例

既存ノートを覆す新ノートを作る場合、双方向に明示する。
- 旧ノート末尾: `## 矛盾するノート` 見出しで `[[新ノート]]` を記載
- 新ノート末尾: `## 上書きする旧見解` 見出しで `[[旧ノート]]` を記載

### 7. 昇格ルール

`references` が 3 件以上に達したノートは `decay: permanent` に格上げする。
繰り返し再確認された知見は鮮度減衰させない。

### 8. 強化記録

既存ノートと 90% 以上重複する内容を書こうとしたら、新規作成せず既存ノートの `references` に今日の日付を追記して「再確認」とする。

### 9. ネガティブリスト (書かない)

- typo / スタイル修正 / 一時的なエラー / 雑談
- 設計判断・ユーザー指針・原則を伴わない単発の作業ログ
- 整合 (low surprise) のものを新規ノートとして書く (references 追記で済ます)

価値ある進化も新規も無ければ何も書かない。

## ディレクトリ構造

```
$KAKERA_HOME/
├── MEMORY.md            # インデックス
├── REVIEW.md            # 復習キュー (週次自動生成)
├── .kakera-config.toml  # ユーザー設定 (recall_style 等)
├── .hook.log            # SessionEnd hook ログ
└── knowledge/
    ├── decisions/   # 技術選定・方針決定
    ├── mistakes/    # エラー・修正指示
    ├── feedback/    # 行動ガイドライン
    ├── design/      # 設計原則
    ├── project/     # プロジェクト固有
    ├── user/        # ユーザー背景
    └── questions/   # 未解決の問い (8 割止め)
```

各カテゴリ直下に hub note (フィードバック.md / 設計.md / ユーザー.md / プロジェクト.md / 意思決定.md / 失敗学習.md / 問い.md) を置く。

## ユーザーの「自分の言葉」原則

記事『コネクティング勉強法』に倣い、AI の説明をそのまま採用させない。
- Claude が説明する時、ユーザーに「自分の言葉で言い直して」とは強制しない (摩擦になる)
- ただし、ユーザーが「自分はこう理解した」と言ったら、その言い方を保存内容に反映する
- 理解できないと表明された内容は採用せず、`questions/` に移す

## 鮮度判定 (検索時の扱い)

- ヒットしたら、過去の文脈・判断理由を踏まえて回答する
- ヒットしなければそのまま進める
- 過去の結論をそのまま適用せず、現在の状況と照合して判断する
- 各ファイルの `decay` と `references` を確認し、古い情報なら現状と照合して更新または削除
